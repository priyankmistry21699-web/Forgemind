# packages/connectors

> External service integrations — OAuth, APIs, databases, cloud services.

## Contents (planned)

- Connector registry and discovery
- OAuth flow handlers (authorization code, client credentials)
- Setup wizard logic (scope guidance, callback URI generation)
- Connection health checks
- Token refresh and rotation
- Connector templates for common services (GitHub, Slack, AWS, etc.)
